# 🧪 .env.sample — образец локальных переменных окружения (НЕ коммитим реальный .env)
BOT_TOKEN=
WEBHOOK_SECRET=
# 🔌 Для локального теста вебхука нужен публичный адрес; но для простоты:
PUBLIC_URL=http://localhost:8000
